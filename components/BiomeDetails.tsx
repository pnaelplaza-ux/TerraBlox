import React from 'react';

// This component has been deprecated as per user request to remove trees and rocks.
// Keeping the file as a placeholder to avoid build errors if referenced elsewhere (though removed from Scene).
export const BiomeDetails: React.FC<any> = () => {
  return null;
};
